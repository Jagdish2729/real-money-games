"use client";
import { FormEvent, useState } from "react";
import type { ReactNode } from "react";
const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:4000";
type Mode = "login" | "register" | "forgot";
const input = "w-full rounded-2xl border border-white/10 bg-[#0c0e13] px-4 py-4 outline-none placeholder:text-white/20 focus:border-white/30";
function Field({label, children}:{label:string;children:ReactNode}) { return <label className="block"><span className="mb-2 block text-sm font-semibold text-white/70">{label}</span>{children}</label>; }
export default function LoginPage() {
  const [mode,setMode]=useState<Mode>("login"); const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [phone,setPhone]=useState(""); const [password,setPassword]=useState(""); const [confirmPassword,setConfirmPassword]=useState(""); const [loading,setLoading]=useState(false); const [error,setError]=useState(""); const [message,setMessage]=useState("");
  const switchMode=(next:Mode)=>{setMode(next);setError("");setMessage("");};
  async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setMessage("");setLoading(true);try{let url="";let body:Record<string,string>={};if(mode==="register"){if(password!==confirmPassword)throw new Error("Passwords do not match");url=`${API_BASE_URL}/auth/register`;body={name,email,phoneNumber:`+91${phone}`,password};}else if(mode==="login"){url=`${API_BASE_URL}/auth/login`;body={phoneNumber:`+91${phone}`,password};}else{url=`${API_BASE_URL}/auth/forgot-password`;body={email};}const r=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw new Error(data.message??"Something went wrong");if(mode==="forgot"){setMessage("If this email is registered, a password reset link has been sent.");return;}localStorage.setItem("accessToken",data.accessToken);localStorage.setItem("user",JSON.stringify(data.user));window.location.href="/games";}catch(err){setError(err instanceof Error?err.message:"Something went wrong");}finally{setLoading(false);}}
  return <main className="min-h-screen bg-[#07080b] px-5 text-white"><div className="mx-auto flex min-h-screen max-w-md flex-col justify-center py-10"><a href="/" className="mb-10 flex items-center gap-3 self-center"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-white text-lg font-black text-black">R</span><span className="font-bold tracking-tight">ROLLRUSH</span></a><section className="rounded-3xl border border-white/10 bg-white/[0.04] p-6 shadow-2xl shadow-black/30 sm:p-8"><div className="mb-8"><p className="text-xs font-bold uppercase tracking-[0.18em] text-white/35">Account</p><h1 className="mt-2 text-3xl font-black tracking-tight">{mode==="register"?"Create your account":mode==="forgot"?"Reset your password":"Welcome back"}</h1><p className="mt-3 text-sm leading-6 text-white/45">{mode==="register"?"Create your RollRush account. No SMS OTP required.":mode==="forgot"?"We’ll send a reset link to your registered email.":"Log in with your mobile number and password."}</p></div><form onSubmit={submit} className="space-y-4">{mode==="register"&&<><Field label="Name"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" className={input}/></Field><Field label="Email"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className={input}/></Field></>}{mode!=="forgot"&&<Field label="Mobile number"><div className="flex overflow-hidden rounded-2xl border border-white/10 bg-[#0c0e13]"><span className="flex items-center border-r border-white/10 px-4 text-sm font-semibold text-white/50">+91</span><input required inputMode="numeric" maxLength={10} value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,""))} placeholder="10-digit number" className="min-w-0 flex-1 bg-transparent px-4 py-4 outline-none placeholder:text-white/20"/></div></Field>}{mode==="forgot"&&<Field label="Email"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Registered email" className={input}/></Field>}{mode!=="forgot"&&<Field label="Password"><input required type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimum 8 characters" className={input}/></Field>}{mode==="register"&&<Field label="Confirm password"><input required type="password" minLength={8} value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} placeholder="Re-enter password" className={input}/></Field>}{error&&<p className="rounded-xl border border-red-400/20 bg-red-400/10 px-4 py-3 text-sm text-red-300">{error}</p>}{message&&<p className="rounded-xl border border-emerald-400/20 bg-emerald-400/10 px-4 py-3 text-sm text-emerald-300">{message}</p>}<button type="submit" disabled={loading} className="w-full rounded-2xl bg-white py-4 text-sm font-bold text-black transition hover:bg-white/90 disabled:opacity-40">{loading?"Please wait...":mode==="register"?"Create account":mode==="forgot"?"Send reset link":"Login"}</button></form><div className="mt-6 flex flex-col items-center gap-3 text-sm">{mode==="login"&&<><button onClick={()=>switchMode("forgot")} className="font-semibold text-white/45 hover:text-white">Forgot password?</button><p className="text-white/35">New here? <button onClick={()=>switchMode("register")} className="font-bold text-white/75">Create account</button></p></>}{mode==="register"&&<p className="text-white/35">Already have an account? <button onClick={()=>switchMode("login")} className="font-bold text-white/75">Login</button></p>}{mode==="forgot"&&<button onClick={()=>switchMode("login")} className="font-semibold text-white/45">← Back to login</button>}</div><p className="mt-7 border-t border-white/10 pt-5 text-center text-xs leading-5 text-white/30">Your password is stored securely as a one-way hash, never as plain text.</p></section><a href="/" className="mt-6 text-center text-sm font-semibold text-white/35">← Back to RollRush</a></div></main>;
}
